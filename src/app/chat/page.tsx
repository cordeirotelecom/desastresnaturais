import ChatGlobal from '@/components/ChatGlobal';

export const metadata = {
  title: 'Chat Global | Sistema de Desastres',
  description: 'Comunicação em tempo real - Todos os usuários logados podem conversar'
};

export default function ChatPage() {
  return <ChatGlobal />;
}
